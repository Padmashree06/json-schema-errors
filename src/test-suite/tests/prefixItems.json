{
  "$schema": "../test-suite.schema.json",

  "description": "The prefixItems keyword",
  "tests": [
    {
      "description": "prefixItems",
      "compatibility": "2020",
      "schema": {
        "prefixItems": [{ "type": "number" }]
      },
      "instance": ["foo"],
      "errors": [
        {
          "messageId": "type-message",
          "messageParams": {
            "expectedTypes": { "or": ["number"] }
          },
          "instanceLocation": "#/0",
          "schemaLocations": ["#/prefixItems/0/type"]
        }
      ]
    },
    {
      "description": "prefixItems with fewer items than are defined",
      "compatibility": "2020",
      "schema": {
        "prefixItems": [
          { "type": "number" },
          { "type": "string" }
        ]
      },
      "instance": ["foo"],
      "errors": [
        {
          "messageId": "type-message",
          "messageParams": {
            "expectedTypes": { "or": ["number"] }
          },
          "instanceLocation": "#/0",
          "schemaLocations": ["#/prefixItems/0/type"]
        }
      ]
    },
    {
      "description": "prefixItems on an non-object",
      "compatibility": "2020",
      "schema": {
        "prefixItems": [{ "type": "number" }]
      },
      "instance": 42,
      "errors": []
    },
    {
      "description": "prefixItems pass",
      "schema": {
        "prefixItems": [{ "type": "number" }]
      },
      "instance": { "foo": 42 },
      "errors": []
    }
  ]
}
